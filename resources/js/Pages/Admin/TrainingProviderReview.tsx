import { router } from '@inertiajs/react'; import { Badge, Card, DashboardLayout } from '@/Components/Madarat';
import { arabicLabel } from '@/lib/arabicLabels';
export default function Review({provider}:any){const reject=()=>{const reason=window.prompt('اكتب سبب الرفض');if(reason)router.post(`/admin/training/providers/${provider.id}/reject`,{reason})};return <DashboardLayout title="مراجعة مقدم التدريب"><Card><div className="flex justify-between"><h2 className="text-xl font-black">{provider.display_name}</h2><Badge tone="cyan">{arabicLabel('providerStatus',provider.verification_status)}</Badge></div><dl className="mt-5 grid gap-3 md:grid-cols-2"><div>النوع: {arabicLabel('providerType',provider.provider_type)}</div><div>المدينة: {provider.city}</div><div>البريد: {provider.email}</div><div>الهاتف: {provider.phone}</div><div className="md:col-span-2">الوصف: {provider.description}</div><div>التخصصات: {provider.specializations?.join('، ')}</div><div>السجل التجاري: {provider.commercial_registration_number||'—'}</div></dl>{provider.rejection_reason&&<p className="mt-4 text-red-600">{provider.rejection_reason}</p>}{provider.verification_status==='pending'&&<div className="mt-5 flex gap-2"><button onClick={()=>router.post(`/admin/training/providers/${provider.id}/verify`)} className="rounded bg-green-600 px-4 py-2 text-white">توثيق</button><button onClick={reject} className="rounded bg-red-600 px-4 py-2 text-white">رفض مع السبب</button></div>}</Card></DashboardLayout>}
