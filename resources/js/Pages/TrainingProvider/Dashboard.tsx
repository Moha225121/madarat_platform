import { Link } from '@inertiajs/react';
import { Badge, Card, DashboardLayout, StatCard, icons } from '@/Components/Madarat';
import { arabicLabel } from '@/lib/arabicLabels';
export default function Dashboard({ provider, stats, recentCourses }: any) {
 return <DashboardLayout title="لوحة مقدم التدريب"><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-xl font-black text-madarat-navy">{provider.display_name}</h2><Badge tone={provider.verification_status === 'verified' ? 'green' : 'cyan'}>{arabicLabel('providerStatus',provider.verification_status)}</Badge></div><Link className="rounded-lg bg-madarat-blue px-4 py-2 font-bold text-white" href="/training/courses/create">إضافة دورة</Link></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><StatCard label="إجمالي الدورات" value={stats.total} icon={icons.BookOpen}/><StatCard label="منشورة" value={stats.published} icon={icons.CheckCircle2}/><StatCard label="قيد المراجعة" value={stats.pending} icon={icons.Clock}/><StatCard label="المشاهدات" value={stats.views} icon={icons.Eye}/></div><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><StatCard label="الترشيحات" value={stats.recommendations} icon={icons.Sparkles}/><StatCard label="المهتمون" value={stats.interested} icon={icons.UserRound}/><StatCard label="متوسط الملاءمة" value={`${stats.average_relevance}%`} icon={icons.Target}/><StatCard label="المرفوضة" value={stats.rejected} icon={icons.XCircle}/></div><Card className="mt-6"><h3 className="font-black">آخر النشاطات</h3><div className="mt-3 space-y-2">{recentCourses.length ? recentCourses.map((c:any)=><Link key={c.id} href={`/training/courses/${c.id}`} className="flex justify-between rounded-lg bg-slate-50 p-3"><span>{c.title}</span><Badge tone="cyan">{arabicLabel('courseStatus',c.status)}</Badge></Link>) : <p className="text-slate-500">لا توجد دورات بعد.</p>}</div></Card></DashboardLayout>;
}
